# real-time-lane-violation-detection
Real-time road lane violation detection system built using Python, OpenCV, and NumPy. Implements Canny Edge Detection, Hough Transform, ROI masking, lane averaging, and temporal smoothing to detect lane violations from video streams. Evaluated using Precision, Recall, and F1-score metrics.
